#include <cstdio>
#include <iostream>

using namespace std;

int main(void)
{
    int repeat;
    scanf("%d",&repeat);
    while(repeat-->) {
        int k;
        scanf("%d",&k);
        for(int i = 0;i<k;i++) {
            
        }
    }
    return 0;
}
